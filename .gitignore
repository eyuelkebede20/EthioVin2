# Local reviewer tooling (Gemini code-review agent) — keep out of version control.
GEMINI.md
GEMINI.template.md
REVIEW_LOG.md

# Secrets — never commit real .env files (keep the .env.example templates).
.env
.env.local
.env.*.local
**/.env
!**/.env.example

# Dependencies & build artifacts
node_modules/
dist/
.next/
*.tsbuildinfo
